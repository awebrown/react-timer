let React = require('react'),
    Timer = React.createClass({
      render: function() {
        return (
          <div>
            Timer.jsx
          </div>
        );
      }
    })

module.exports = Timer;
